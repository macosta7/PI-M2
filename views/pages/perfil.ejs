<!DOCTYPE html>
<html>
<head>
  <title>Meu Perfil - Checkin Room</title>
  <meta charset="UTF-8">
</head>
<body>
  <h1>Editar Perfil</h1>

  <form action="/usuarios/<%= usuario.id_usuario %>" method="POST">
    <!-- O método correto seria PUT, mas HTML só suporta GET e POST -->
    <!-- Use override com método PUT no backend se desejar -->

    <label for="nome">Nome:</label><br>
    <input type="text" name="nome" id="nome" value="<%= usuario.nm_usuario %>" required><br><br>

    <label for="email">Email:</label><br>
    <input type="email" name="email" id="email" value="<%= usuario.email_usuario %>" required><br><br>

    <label for="ocupacao">Ocupação:</label><br>
    <select name="ocupacao" id="ocupacao" required>
      <option value="aluno" <%= usuario.ocupacao_usuario === 'aluno' ? 'selected' : '' %>>Aluno</option>
      <option value="professor" <%= usuario.ocupacao_usuario === 'professor' ? 'selected' : '' %>>Professor</option>
      <option value="coordenador" <%= usuario.ocupacao_usuario === 'coordenador' ? 'selected' : '' %>>Coordenador</option>
      <option value="recepcao" <%= usuario.ocupacao_usuario === 'recepcao' ? 'selected' : '' %>>Recepção</option>
    </select><br><br>

    <label for="senha">Senha:</label><br>
    <input type="password" name="senha" id="senha" required><br><br>

    <button type="submit">Salvar Alterações</button>
  </form>

  <p><a href="/reservas/usuario/<%= usuario.id_usuario %>">Ver Minhas Reservas</a></p>
</body>
</html>
